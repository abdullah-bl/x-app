import { Metadata } from "next"

export { default } from "~/components/layout/index"

export const metadata: Metadata = {
  title: "Projects Overview",
  description: "Here are the projects you have access to.",
}
